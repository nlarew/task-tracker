mutation DeleteTask($taskId: ObjectId!) {
  deletedTask: deleteOneTask(query: { _id: $taskId }) {
    _id
    description
    status
    assignee {
      _id
      name
      image
      user_id
    }
  }
}
